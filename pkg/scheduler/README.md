# pkg/scheduler

Omega-model placement scheduler: plugin pipeline (`Plugin` = `Name`/`Filter`/`Score`/`Bind`) with cost-aware GPU placement (`CostAwareGPUPlacement`) and attestation-gated admission (`AttestationGate`).

See docs/architecture/PHASE_8C_INTEGRATION.md
