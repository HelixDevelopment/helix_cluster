# pkg/marketplace

GPU/inference marketplace adapter layer: provider registration (`ChutesAdapter`), weighted placement ranking (`CompositeScorer`), per-class capacity metering (`GPUReservationTable`), and confidential-path seams (`Encryptor`/`Attestor`).

See docs/architecture/PHASE_8C_INTEGRATION.md
