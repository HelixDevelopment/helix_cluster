{"stats":{"totalMutantsCount":107,"killedCount":51,"notCoveredCount":0,"escapedCount":56,"errorCount":0,"skippedCount":0,"timeOutCount":0,"msi":0.4766355140186916,"mutationCodeCoverage":0,"coveredCodeMsi":0},"escaped":[{"mutator":{"mutatorName":"branch/else","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\t_, _, _ = missingTables, missingTables, tbl\n\t\t}\n\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":216},"diff":"--- Original\n+++ New\n@@ -213,8 +213,9 @@\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n-\t\t\tmissingTables = append(missingTables, tbl)\n+\t\t\t_, _, _ = missingTables, missingTables, tbl\n \t\t}\n+\n \t}\n \tfacts = append(facts, StructuralFact{\n \t\tName:   \"15 required tables\",\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.10\" with checksum f92e23c045063a2cb1f3857a9948454f\n"},{"mutator":{"mutatorName":"branch/else","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\t_, _, _ = name, cmdArgs, conn\n\t}\n\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":445},"diff":"--- Original\n+++ New\n@@ -442,9 +442,9 @@\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n \t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n \t} else {\n-\t\tname = \"psql\"\n-\t\tcmdArgs = append(conn, \"-f\", \"-\")\n+\t\t_, _, _ = name, cmdArgs, conn\n \t}\n+\n \tcmd := exec.Command(name, cmdArgs...)\n \tcmd.Stdin = strings.NewReader(sqlText)\n \tvar buf bytes.Buffer\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.11\" with checksum 39c038d62859d3093bcca5e840fe1f4c\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\t_ = fmt.Errorf\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":105},"diff":"--- Original\n+++ New\n@@ -102,7 +102,7 @@\n func SchemaPath() (string, error) {\n \t_, callerFile, _, ok := runtime.Caller(0)\n \tif !ok {\n-\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n+\t\t_ = fmt.Errorf\n \t}\n \t// callerFile is .../internal/schema/schema.go\n \t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.12\" with checksum fd73966ebb31d1faddee93682cae9e6f\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\t_ = err\n\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":118},"diff":"--- Original\n+++ New\n@@ -115,7 +115,8 @@\n func ReadSQL() (string, error) {\n \tp, err := SchemaPath()\n \tif err != nil {\n-\t\treturn \"\", err\n+\t\t_ = err\n+\n \t}\n \tdata, err := os.ReadFile(p)\n \tif err != nil {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.13\" with checksum cc82b7bf5a6f8c18a3fd3163be784012\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\t_, _, _ = fmt.Errorf, p, err\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":122},"diff":"--- Original\n+++ New\n@@ -119,7 +119,7 @@\n \t}\n \tdata, err := os.ReadFile(p)\n \tif err != nil {\n-\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n+\t\t_, _, _ = fmt.Errorf, p, err\n \t}\n \treturn string(data), nil\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.14\" with checksum 99d1132c606c12ca83b1ad9f4dd6cad7\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\t_ = fmt.Errorf\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":133},"diff":"--- Original\n+++ New\n@@ -130,7 +130,7 @@\n func MigrationsDir() (string, error) {\n \t_, callerFile, _, ok := runtime.Caller(0)\n \tif !ok {\n-\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n+\t\t_ = fmt.Errorf\n \t}\n \trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n \treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.15\" with checksum 8189d01c76a5bc297ce13466a698f5c2\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\t_ = err\n\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":146},"diff":"--- Original\n+++ New\n@@ -143,7 +143,8 @@\n func ChainUpFiles() ([]string, error) {\n \tdir, err := MigrationsDir()\n \tif err != nil {\n-\t\treturn nil, err\n+\t\t_ = err\n+\n \t}\n \tentries, err := os.ReadDir(dir)\n \tif err != nil {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.16\" with checksum edc910e3c41e56031aa832008d6c6acd\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\t_, _, _ = fmt.Errorf, dir, err\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":150},"diff":"--- Original\n+++ New\n@@ -147,7 +147,7 @@\n \t}\n \tentries, err := os.ReadDir(dir)\n \tif err != nil {\n-\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n+\t\t_, _, _ = fmt.Errorf, dir, err\n \t}\n \tvar files []string\n \tfor _, e := range entries {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.17\" with checksum 147ca0d60a6668445dbfe0827d25b4a6\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":161},"diff":"--- Original\n+++ New\n@@ -158,7 +158,7 @@\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n \t\t// but guard explicitly in case of future renames).\n \t\tif name == \"0001_primary_schema.sql\" {\n-\t\t\tcontinue\n+\n \t\t}\n \t\tfiles = append(files, filepath.Join(dir, name))\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.19\" with checksum d488dbe81d35bcbd1ab19325b116240d\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\t_ = err\n\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":175},"diff":"--- Original\n+++ New\n@@ -172,7 +172,8 @@\n func ReadChainSQL() (string, error) {\n \tfiles, err := ChainUpFiles()\n \tif err != nil {\n-\t\treturn \"\", err\n+\t\t_ = err\n+\n \t}\n \tvar b strings.Builder\n \tfor _, f := range files {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.20\" with checksum 70945047779d051eda41e837b074d693\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\t_, _, _ = fmt.Errorf, f, err\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":181},"diff":"--- Original\n+++ New\n@@ -178,7 +178,7 @@\n \tfor _, f := range files {\n \t\tdata, err := os.ReadFile(f)\n \t\tif err != nil {\n-\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n+\t\t\t_, _, _ = fmt.Errorf, f, err\n \t\t}\n \t\tb.Write(data)\n \t\tb.WriteByte('\\n')\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.21\" with checksum f2d6b3c10c563473def49f9e19bbde82\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\t_, _ = db, dsnDB\n\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":338},"diff":"--- Original\n+++ New\n@@ -335,7 +335,8 @@\n \n \tdb := os.Getenv(\"PGDATABASE\")\n \tif db == \"\" {\n-\t\tdb = dsnDB\n+\t\t_, _ = db, dsnDB\n+\n \t}\n \tif db == \"\" {\n \t\tdb = \"helix\"\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.25\" with checksum 963140962da916d8c13374825cfd11d0\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\t_ = db\n\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":341},"diff":"--- Original\n+++ New\n@@ -338,7 +338,8 @@\n \t\tdb = dsnDB\n \t}\n \tif db == \"\" {\n-\t\tdb = \"helix\"\n+\t\t_ = db\n+\n \t}\n \n \tuser := os.Getenv(\"PGUSER\")\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.26\" with checksum 34f36e1009359ba42cd60a5c759c9e1e\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\t_, _ = user, dsnUser\n\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":346},"diff":"--- Original\n+++ New\n@@ -343,7 +343,8 @@\n \n \tuser := os.Getenv(\"PGUSER\")\n \tif user == \"\" {\n-\t\tuser = dsnUser\n+\t\t_, _ = user, dsnUser\n+\n \t}\n \tif user == \"\" {\n \t\tuser = \"helix\"\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.27\" with checksum b3d32af20c1df35e91179dba83fe3c59\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\t_ = user\n\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":349},"diff":"--- Original\n+++ New\n@@ -346,7 +346,8 @@\n \t\tuser = dsnUser\n \t}\n \tif user == \"\" {\n-\t\tuser = \"helix\"\n+\t\t_ = user\n+\n \t}\n \n \tif container != \"\" {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.28\" with checksum 81177674225f288e6ae66b1a1edecda5\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":367},"diff":"--- Original\n+++ New\n@@ -364,7 +364,7 @@\n // libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\n func parseDSNUserDB(dsn string) (user, db string) {\n \tif dsn == \"\" {\n-\t\treturn \"\", \"\"\n+\n \t}\n \tu, err := url.Parse(dsn)\n \tif err != nil {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.31\" with checksum a22c468aedac64981e4b3d9a60aa5edf\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":371},"diff":"--- Original\n+++ New\n@@ -368,7 +368,7 @@\n \t}\n \tu, err := url.Parse(dsn)\n \tif err != nil {\n-\t\treturn \"\", \"\"\n+\n \t}\n \tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n \t\treturn \"\", \"\"\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.32\" with checksum 5bbec0e0262a0a4e5aaa6becac12d401\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":374},"diff":"--- Original\n+++ New\n@@ -371,7 +371,7 @@\n \t\treturn \"\", \"\"\n \t}\n \tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n-\t\treturn \"\", \"\"\n+\n \t}\n \tif u.User != nil {\n \t\tuser = u.User.Username()\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.33\" with checksum 162b020ffd66b25faec3f71450919bad\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\t_ = user\n\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":397},"diff":"--- Original\n+++ New\n@@ -394,7 +394,8 @@\n \t}\n \tuser := cfg.User\n \tif user == \"\" {\n-\t\tuser = \"helix\"\n+\t\t_ = user\n+\n \t}\n \tdb := cfg.Database\n \tif db == \"\" {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.36\" with checksum 3176f912054d0b9f211d464c04d8d470\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\t_ = db\n\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":401},"diff":"--- Original\n+++ New\n@@ -398,7 +398,8 @@\n \t}\n \tdb := cfg.Database\n \tif db == \"\" {\n-\t\tdb = \"helix\"\n+\t\t_ = db\n+\n \t}\n \treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.37\" with checksum fa120feba3dff0665c2a71783b68c196\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\t_ = err\n\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":427},"diff":"--- Original\n+++ New\n@@ -424,7 +424,8 @@\n func ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n \tsqlText, err := ReadSQL()\n \tif err != nil {\n-\t\treturn \"\", err\n+\t\t_ = err\n+\n \t}\n \treturn RunSQLStdin(cfg, sqlText)\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.38\" with checksum 299274cd96504ca9e19420210c36c6ad\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\t_, _, _, _, _, _ = name, cmdArgs, cfg.ContainerName, conn, cmdArgs, cmdArgs\n\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":441},"diff":"--- Original\n+++ New\n@@ -438,9 +438,8 @@\n \t)\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n-\t\tname = \"podman\"\n-\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n-\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n+\t\t_, _, _, _, _, _ = name, cmdArgs, cfg.ContainerName, conn, cmdArgs, cmdArgs\n+\n \t} else {\n \t\tname = \"psql\"\n \t\tcmdArgs = append(conn, \"-f\", \"-\")\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.39\" with checksum cfe3ec1144e1f815a0e327ec52242c0f\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":454},"diff":"--- Original\n+++ New\n@@ -451,7 +451,7 @@\n \tcmd.Stdout = \u0026buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n-\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n+\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n \t}\n \treturn buf.String(), nil\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.40\" with checksum 25c2f5f8593e6b296a6cb789d8242f77\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\t_ = err\n\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":463},"diff":"--- Original\n+++ New\n@@ -460,7 +460,8 @@\n func QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n \tout, err := runPsqlQuery(cfg, sql)\n \tif err != nil {\n-\t\treturn nil, err\n+\t\t_ = err\n+\n \t}\n \tvar rows []string\n \tfor _, line := range strings.Split(out, \"\\n\") {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.41\" with checksum 4dc1972acb9edaeeece917b9c14171b2\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\t_, _, _ = rows, rows, line\n\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":469},"diff":"--- Original\n+++ New\n@@ -466,7 +466,8 @@\n \tfor _, line := range strings.Split(out, \"\\n\") {\n \t\tline = strings.TrimSpace(line)\n \t\tif line != \"\" {\n-\t\t\trows = append(rows, line)\n+\t\t\t_, _, _ = rows, rows, line\n+\n \t\t}\n \t}\n \treturn rows, nil\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.42\" with checksum f0fc6119be32fd883ed255fd99507743\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":483},"diff":"--- Original\n+++ New\n@@ -480,7 +480,7 @@\n \tcmd.Stdout = \u0026buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n-\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n+\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n \t}\n \treturn buf.String(), nil\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.43\" with checksum 4334e976736d13317a9066efe3ddbf82\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\t_, _, _, _, _, _, _, _, _ = name, cmdArgs, cfg.ContainerName, conn, cmdArgs, cmdArgs, extraArgs, name, cmdArgs\n\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":495},"diff":"--- Original\n+++ New\n@@ -492,10 +492,8 @@\n func buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n-\t\tname = \"podman\"\n-\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n-\t\tcmdArgs = append(cmdArgs, extraArgs...)\n-\t\treturn name, cmdArgs\n+\t\t_, _, _, _, _, _, _, _, _ = name, cmdArgs, cfg.ContainerName, conn, cmdArgs, cmdArgs, extraArgs, name, cmdArgs\n+\n \t}\n \tname = \"psql\"\n \tcmdArgs = append(conn, extraArgs...)\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.44\" with checksum 77c2755eba211dc248d04a3a62e5be44\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":513},"diff":"--- Original\n+++ New\n@@ -510,7 +510,7 @@\n \tcmd.Stdout = \u0026buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n-\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n+\t\t_, _, _, _ = buf.String, fmt.Errorf, err, buf.String\n \t}\n \treturn buf.String(), nil\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.45\" with checksum bcff7383ac9886e0cf7bcdc1a8bf41b7\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif false || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":155},"diff":"--- Original\n+++ New\n@@ -152,7 +152,7 @@\n \tvar files []string\n \tfor _, e := range entries {\n \t\tname := e.Name()\n-\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n+\t\tif false || !strings.HasSuffix(name, \".up.sql\") {\n \t\t\tcontinue\n \t\t}\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.46\" with checksum 82560077b53336fb1240a4d212c9b6cf\n"},{"mutator":{"mutatorName":"loop/break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tbreak\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":161},"diff":"--- Original\n+++ New\n@@ -158,7 +158,7 @@\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n \t\t// but guard explicitly in case of future renames).\n \t\tif name == \"0001_primary_schema.sql\" {\n-\t\t\tcontinue\n+\t\t\tbreak\n \t\t}\n \t\tfiles = append(files, filepath.Join(dir, name))\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.55\" with checksum fefd5f873f5c82c7fa229f990dc2c3a2\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tbreak\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":237},"diff":"--- Original\n+++ New\n@@ -234,6 +234,7 @@\n \n \t// Fact 3: required trigger names present\n \tfor _, trig := range RequiredTriggers {\n+\t\tbreak\n \t\tupperTrig := strings.ToUpper(trig)\n \t\tfacts = append(facts, StructuralFact{\n \t\t\tName:   \"trigger: \" + trig,\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.60\" with checksum ffb523dc550338dbeec31e63bb9a4241\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tbreak\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":247},"diff":"--- Original\n+++ New\n@@ -244,6 +244,7 @@\n \n \t// Fact 4: representative index names\n \tfor _, idx := range RequiredIndexPrefixes {\n+\t\tbreak\n \t\tupperIdx := strings.ToUpper(idx)\n \t\tfacts = append(facts, StructuralFact{\n \t\t\tName:   \"index: \" + idx,\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.61\" with checksum 53183fab4fffe8b18c9435467274a455\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tbreak\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":467},"diff":"--- Original\n+++ New\n@@ -464,11 +464,13 @@\n \t}\n \tvar rows []string\n \tfor _, line := range strings.Split(out, \"\\n\") {\n+\t\tbreak\n \t\tline = strings.TrimSpace(line)\n \t\tif line != \"\" {\n \t\t\trows = append(rows, line)\n \t\t}\n \t}\n+\n \treturn rows, nil\n }\n \n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.64\" with checksum c64065e12f0fc7f0e989a5f10aa85960\n"},{"mutator":{"mutatorName":"numbers/incrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(1)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":103},"diff":"--- Original\n+++ New\n@@ -100,7 +100,7 @@\n // resolved relative to the caller's source file location (works regardless of\n // working directory).\n func SchemaPath() (string, error) {\n-\t_, callerFile, _, ok := runtime.Caller(0)\n+\t_, callerFile, _, ok := runtime.Caller(1)\n \tif !ok {\n \t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.70\" with checksum 07824767e3c3f1ddfb05f07ffe2845e0\n"},{"mutator":{"mutatorName":"numbers/incrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(1)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":131},"diff":"--- Original\n+++ New\n@@ -128,7 +128,7 @@\n // (migrations/postgresql/), resolved relative to this source file so it works\n // regardless of the process working directory.\n func MigrationsDir() (string, error) {\n-\t_, callerFile, _, ok := runtime.Caller(0)\n+\t_, callerFile, _, ok := runtime.Caller(1)\n \tif !ok {\n \t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.71\" with checksum 7c1a516f2b846e7b83704581b0bafcdb\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\t_, _ = sort.Strings, files\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":165},"diff":"--- Original\n+++ New\n@@ -162,7 +162,7 @@\n \t\t}\n \t\tfiles = append(files, filepath.Join(dir, name))\n \t}\n-\tsort.Strings(files)\n+\t_, _ = sort.Strings, files\n \treturn files, nil\n }\n \n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.75\" with checksum d190bfd905a5fe74ceb9a615dab581d2\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\t_ = b.WriteByte\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":184},"diff":"--- Original\n+++ New\n@@ -181,7 +181,7 @@\n \t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n \t\t}\n \t\tb.Write(data)\n-\t\tb.WriteByte('\\n')\n+\t\t_ = b.WriteByte\n \t}\n \treturn b.String(), nil\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.78\" with checksum 0a69990d4ef18212c0fa417ff7f5a6bf\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\t_, _, _ = missingTables, missingTables, tbl\n\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":216},"diff":"--- Original\n+++ New\n@@ -213,7 +213,8 @@\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n-\t\t\tmissingTables = append(missingTables, tbl)\n+\t\t\t_, _, _ = missingTables, missingTables, tbl\n+\n \t\t}\n \t}\n \tfacts = append(facts, StructuralFact{\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.84\" with checksum aaa483591e6263019238c928e8766ec0\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\t_, _, _ = cmd.Stdin, strings.NewReader, sqlText\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":449},"diff":"--- Original\n+++ New\n@@ -446,7 +446,7 @@\n \t\tcmdArgs = append(conn, \"-f\", \"-\")\n \t}\n \tcmd := exec.Command(name, cmdArgs...)\n-\tcmd.Stdin = strings.NewReader(sqlText)\n+\t_, _, _ = cmd.Stdin, strings.NewReader, sqlText\n \tvar buf bytes.Buffer\n \tcmd.Stdout = \u0026buf\n \tcmd.Stderr = \u0026buf\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.99\" with checksum 8b6dbfca1b7485edfd2408e8639ab93d\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\t_, _ = cmd.Stdout, buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":451},"diff":"--- Original\n+++ New\n@@ -448,7 +448,7 @@\n \tcmd := exec.Command(name, cmdArgs...)\n \tcmd.Stdin = strings.NewReader(sqlText)\n \tvar buf bytes.Buffer\n-\tcmd.Stdout = \u0026buf\n+\t_, _ = cmd.Stdout, buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.100\" with checksum f4184ce6478d80581c9b2376d213311a\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\t_, _ = cmd.Stderr, buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":452},"diff":"--- Original\n+++ New\n@@ -449,7 +449,7 @@\n \tcmd.Stdin = strings.NewReader(sqlText)\n \tvar buf bytes.Buffer\n \tcmd.Stdout = \u0026buf\n-\tcmd.Stderr = \u0026buf\n+\t_, _ = cmd.Stderr, buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.101\" with checksum e3b3581ed475a01ca8842a4cf2ddef96\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\t_ = name\n\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":441},"diff":"--- Original\n+++ New\n@@ -438,7 +438,8 @@\n \t)\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n-\t\tname = \"podman\"\n+\t\t_ = name\n+\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n \t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n \t} else {\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.102\" with checksum b9d4cdfc6962bdd50af7e8338fca08d9\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\t_, _, _ = cmdArgs, cfg.ContainerName, conn\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":442},"diff":"--- Original\n+++ New\n@@ -439,7 +439,7 @@\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n \t\tname = \"podman\"\n-\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n+\t\t_, _, _ = cmdArgs, cfg.ContainerName, conn\n \t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n \t} else {\n \t\tname = \"psql\"\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.103\" with checksum f23649604d0454437a4ad69f2426c528\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\t_, _ = cmdArgs, cmdArgs\n\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":443},"diff":"--- Original\n+++ New\n@@ -440,7 +440,8 @@\n \tif cfg.UseContainer {\n \t\tname = \"podman\"\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n-\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n+\t\t_, _ = cmdArgs, cmdArgs\n+\n \t} else {\n \t\tname = \"psql\"\n \t\tcmdArgs = append(conn, \"-f\", \"-\")\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.104\" with checksum 82a0c5707eaf72d1b2e2bd922a33e9a9\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\t_ = name\n\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":445},"diff":"--- Original\n+++ New\n@@ -442,7 +442,8 @@\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n \t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n \t} else {\n-\t\tname = \"psql\"\n+\t\t_ = name\n+\n \t\tcmdArgs = append(conn, \"-f\", \"-\")\n \t}\n \tcmd := exec.Command(name, cmdArgs...)\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.105\" with checksum 8df9b074d8642cb855733d27f7fabb05\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\t_, _ = cmdArgs, conn\n\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":446},"diff":"--- Original\n+++ New\n@@ -443,7 +443,8 @@\n \t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n \t} else {\n \t\tname = \"psql\"\n-\t\tcmdArgs = append(conn, \"-f\", \"-\")\n+\t\t_, _ = cmdArgs, conn\n+\n \t}\n \tcmd := exec.Command(name, cmdArgs...)\n \tcmd.Stdin = strings.NewReader(sqlText)\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.106\" with checksum 4d5a8331eb37b5d09265b76c5c6d6401\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\t_, _, _ = line, strings.TrimSpace, line\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":467},"diff":"--- Original\n+++ New\n@@ -464,7 +464,7 @@\n \t}\n \tvar rows []string\n \tfor _, line := range strings.Split(out, \"\\n\") {\n-\t\tline = strings.TrimSpace(line)\n+\t\t_, _, _ = line, strings.TrimSpace, line\n \t\tif line != \"\" {\n \t\t\trows = append(rows, line)\n \t\t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.107\" with checksum 0dd66ef0b52a5809c250e304a110af92\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\t_, _ = cmd.Stdout, buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":480},"diff":"--- Original\n+++ New\n@@ -477,7 +477,7 @@\n \tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n \tcmd := exec.Command(name, cmdArgs...)\n \tvar buf bytes.Buffer\n-\tcmd.Stdout = \u0026buf\n+\t_, _ = cmd.Stdout, buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.109\" with checksum ac677a518b7e3459f2601e4034204658\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\t_, _ = cmd.Stderr, buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":481},"diff":"--- Original\n+++ New\n@@ -478,7 +478,7 @@\n \tcmd := exec.Command(name, cmdArgs...)\n \tvar buf bytes.Buffer\n \tcmd.Stdout = \u0026buf\n-\tcmd.Stderr = \u0026buf\n+\t_, _ = cmd.Stderr, buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.110\" with checksum 70b9aedd1b63ff0cf3638aa5d35f76d1\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\t_ = name\n\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":500},"diff":"--- Original\n+++ New\n@@ -497,7 +497,8 @@\n \t\tcmdArgs = append(cmdArgs, extraArgs...)\n \t\treturn name, cmdArgs\n \t}\n-\tname = \"psql\"\n+\t_ = name\n+\n \tcmdArgs = append(conn, extraArgs...)\n \treturn name, cmdArgs\n }\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.111\" with checksum 5a998e77dafaefd1d2d12264fe809b7a\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\t_, _, _ = cmdArgs, conn, extraArgs\n\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":501},"diff":"--- Original\n+++ New\n@@ -498,7 +498,8 @@\n \t\treturn name, cmdArgs\n \t}\n \tname = \"psql\"\n-\tcmdArgs = append(conn, extraArgs...)\n+\t_, _, _ = cmdArgs, conn, extraArgs\n+\n \treturn name, cmdArgs\n }\n \n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.112\" with checksum acf51e622d567d8b2ce887e675d4b01c\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\t_ = name\n\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":495},"diff":"--- Original\n+++ New\n@@ -492,7 +492,8 @@\n func buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n-\t\tname = \"podman\"\n+\t\t_ = name\n+\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n \t\tcmdArgs = append(cmdArgs, extraArgs...)\n \t\treturn name, cmdArgs\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.113\" with checksum d9a06a4cdea33a2496bfe2866327f681\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\t_, _, _ = cmdArgs, cfg.ContainerName, conn\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":496},"diff":"--- Original\n+++ New\n@@ -493,7 +493,7 @@\n \tconn := psqlConnArgs(cfg)\n \tif cfg.UseContainer {\n \t\tname = \"podman\"\n-\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n+\t\t_, _, _ = cmdArgs, cfg.ContainerName, conn\n \t\tcmdArgs = append(cmdArgs, extraArgs...)\n \t\treturn name, cmdArgs\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.114\" with checksum 562bb95b1d729ae51b6552b9fdc3c38a\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\t_, _, _ = cmdArgs, cmdArgs, extraArgs\n\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":497},"diff":"--- Original\n+++ New\n@@ -494,7 +494,8 @@\n \tif cfg.UseContainer {\n \t\tname = \"podman\"\n \t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n-\t\tcmdArgs = append(cmdArgs, extraArgs...)\n+\t\t_, _, _ = cmdArgs, cmdArgs, extraArgs\n+\n \t\treturn name, cmdArgs\n \t}\n \tname = \"psql\"\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.115\" with checksum 70564555b04f7cdf80a3d250d8f1f2f9\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\t_, _ = cmd.Stdout, buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":510},"diff":"--- Original\n+++ New\n@@ -507,7 +507,7 @@\n \tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n \tcmd := exec.Command(name, cmdArgs...)\n \tvar buf bytes.Buffer\n-\tcmd.Stdout = \u0026buf\n+\t_, _ = cmd.Stdout, buf\n \tcmd.Stderr = \u0026buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.116\" with checksum 868a56267524541a56bf5e5209c7c7b5\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\t_, _ = cmd.Stderr, buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":511},"diff":"--- Original\n+++ New\n@@ -508,7 +508,7 @@\n \tcmd := exec.Command(name, cmdArgs...)\n \tvar buf bytes.Buffer\n \tcmd.Stdout = \u0026buf\n-\tcmd.Stderr = \u0026buf\n+\t_, _ = cmd.Stderr, buf\n \tif err := cmd.Run(); err != nil {\n \t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n \t}\n","processOutput":"FAIL \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.117\" with checksum f8bde87854b4941e038726d3e1db9ecf\n"}],"timeouted":null,"killed":[{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":210},"diff":"--- Original\n+++ New\n@@ -207,7 +207,7 @@\n \tfor _, tbl := range RequiredTables {\n \t\tupperTbl := strings.ToUpper(tbl)\n \t\t// Match with trailing space or newline (word boundary after table name)\n-\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n+\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.0\" with checksum 4e18d5fa782280156951bef2d63e6712\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":210},"diff":"--- Original\n+++ New\n@@ -207,7 +207,7 @@\n \tfor _, tbl := range RequiredTables {\n \t\tupperTbl := strings.ToUpper(tbl)\n \t\t// Match with trailing space or newline (word boundary after table name)\n-\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n+\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.1\" with checksum 2dcb46bd10319cfdcd75461b8e636afe\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":211},"diff":"--- Original\n+++ New\n@@ -208,7 +208,7 @@\n \t\tupperTbl := strings.ToUpper(tbl)\n \t\t// Match with trailing space or newline (word boundary after table name)\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n-\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n+\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.2\" with checksum 9ccce56cf1b799096b9099206797e3d1\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":211},"diff":"--- Original\n+++ New\n@@ -208,7 +208,7 @@\n \t\tupperTbl := strings.ToUpper(tbl)\n \t\t// Match with trailing space or newline (word boundary after table name)\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n-\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n+\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.3\" with checksum f686d38848c7d93838a60bcba0f75739\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":212},"diff":"--- Original\n+++ New\n@@ -209,7 +209,7 @@\n \t\t// Match with trailing space or newline (word boundary after table name)\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n-\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n+\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl - \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.4\" with checksum 28d3def9112896b42f194bae111260d4\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":212},"diff":"--- Original\n+++ New\n@@ -209,7 +209,7 @@\n \t\t// Match with trailing space or newline (word boundary after table name)\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n-\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n+\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" - upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.5\" with checksum 6bc5f8ee5b91a48f2081a753476dc499\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" - fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":229},"diff":"--- Original\n+++ New\n@@ -226,7 +226,7 @@\n \tfor _, fn := range RequiredFunctions {\n \t\tupperFn := strings.ToUpper(fn)\n \t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"function: \" + fn,\n+\t\t\tName:   \"function: \" - fn,\n \t\t\tPassed: strings.Contains(upper, upperFn),\n \t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n \t\t})\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.6\" with checksum 2a765901ba24b08f87445d6f5220c199\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" - trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":239},"diff":"--- Original\n+++ New\n@@ -236,7 +236,7 @@\n \tfor _, trig := range RequiredTriggers {\n \t\tupperTrig := strings.ToUpper(trig)\n \t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"trigger: \" + trig,\n+\t\t\tName:   \"trigger: \" - trig,\n \t\t\tPassed: strings.Contains(upper, upperTrig),\n \t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n \t\t})\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.7\" with checksum 1f55e132138602c2143f9ac2f4188dba\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" - idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":249},"diff":"--- Original\n+++ New\n@@ -246,7 +246,7 @@\n \tfor _, idx := range RequiredIndexPrefixes {\n \t\tupperIdx := strings.ToUpper(idx)\n \t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"index: \" + idx,\n+\t\t\tName:   \"index: \" - idx,\n \t\t\tPassed: strings.Contains(upper, upperIdx),\n \t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n \t\t})\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.8\" with checksum 4963d253bec6b09f92495962e6b71735\n"},{"mutator":{"mutatorName":"arithmetic/base","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" - chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":259},"diff":"--- Original\n+++ New\n@@ -256,7 +256,7 @@\n \tfor _, chk := range RequiredCheckConstraints {\n \t\tupperChk := strings.ToUpper(chk)\n \t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"CHECK: \" + chk,\n+\t\t\tName:   \"CHECK: \" - chk,\n \t\t\tPassed: strings.Contains(upper, upperChk),\n \t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n \t\t})\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.9\" with checksum 918e4615ccdab6fe104fe23ecbc21af7\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":156},"diff":"--- Original\n+++ New\n@@ -153,7 +153,7 @@\n \tfor _, e := range entries {\n \t\tname := e.Name()\n \t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n-\t\t\tcontinue\n+\n \t\t}\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n \t\t// but guard explicitly in case of future renames).\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.18\" with checksum ac381d49e328f7a590a7915ba712cc73\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\t_ = tableCount\n\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":214},"diff":"--- Original\n+++ New\n@@ -211,7 +211,8 @@\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n \t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n-\t\t\ttableCount++\n+\t\t\t_ = tableCount\n+\n \t\t} else {\n \t\t\tmissingTables = append(missingTables, tbl)\n \t\t}\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.22\" with checksum b5fe78ecf017ac0015af1d770c0fec43\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\t_, _, _, _, _ = failures, failures, fmt.Sprintf, f.Name, f.Detail\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":294},"diff":"--- Original\n+++ New\n@@ -291,7 +291,7 @@\n \tvar failures []string\n \tfor _, f := range facts {\n \t\tif !f.Passed {\n-\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n+\t\t\t_, _, _, _, _ = failures, failures, fmt.Sprintf, f.Name, f.Detail\n \t\t}\n \t}\n \treturn len(failures) == 0, failures\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.23\" with checksum 4b03bacbc067f4280925e77ab2f8058e\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\t_, _ = dsn, os.Getenv\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":330},"diff":"--- Original\n+++ New\n@@ -327,7 +327,7 @@\n \tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n \tdsn := os.Getenv(\"PSQL_DSN\")\n \tif dsn == \"\" {\n-\t\tdsn = os.Getenv(\"DATABASE_URL\")\n+\t\t_, _ = dsn, os.Getenv\n \t}\n \n \t// Derive defaults from the DSN first, then allow explicit env overrides.\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.24\" with checksum 7366b969dc2628069a0c20279c5580d3\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\t_, _, _, _, _, _, _, _, _ = UseContainer, ContainerName, container, Database, db, User, user, DSN, dsn\n\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":353},"diff":"--- Original\n+++ New\n@@ -350,7 +350,8 @@\n \t}\n \n \tif container != \"\" {\n-\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n+\t\t_, _, _, _, _, _, _, _, _ = UseContainer, ContainerName, container, Database, db, User, user, DSN, dsn\n+\n \t}\n \tif dsn != \"\" {\n \t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.29\" with checksum 33449a2756d0866dc6aba9d25fc4cdac\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\t_, _, _, _, _, _, _ = UseContainer, DSN, dsn, Database, db, User, user\n\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":356},"diff":"--- Original\n+++ New\n@@ -353,7 +353,8 @@\n \t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n \t}\n \tif dsn != \"\" {\n-\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n+\t\t_, _, _, _, _, _, _ = UseContainer, DSN, dsn, Database, db, User, user\n+\n \t}\n \treturn PSQLConfig{}, false\n }\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.30\" with checksum d9eed122198a9c650d2203f187b4f53e\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\t_, _ = user, u.User.Username\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":377},"diff":"--- Original\n+++ New\n@@ -374,7 +374,7 @@\n \t\treturn \"\", \"\"\n \t}\n \tif u.User != nil {\n-\t\tuser = u.User.Username()\n+\t\t_, _ = user, u.User.Username\n \t}\n \tdb = strings.TrimPrefix(u.Path, \"/\")\n \treturn user, db\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.34\" with checksum 578900d9fb6b21a5734efc91644f912b\n"},{"mutator":{"mutatorName":"branch/if","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\t_ = cfg.DSN\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":393},"diff":"--- Original\n+++ New\n@@ -390,7 +390,7 @@\n // every transport fails loudly on the first SQL error.\n func psqlConnArgs(cfg PSQLConfig) []string {\n \tif cfg.DSN != \"\" {\n-\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n+\t\t_ = cfg.DSN\n \t}\n \tuser := cfg.User\n \tif user == \"\" {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.35\" with checksum 73dae47d2d8199a1a2ab555292581a92\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || false {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":155},"diff":"--- Original\n+++ New\n@@ -152,7 +152,7 @@\n \tvar files []string\n \tfor _, e := range entries {\n \t\tname := e.Name()\n-\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n+\t\tif e.IsDir() || false {\n \t\t\tcontinue\n \t\t}\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.47\" with checksum fbe1c0de5fcd6b8ad60dc8a529210dfd\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif false || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":213},"diff":"--- Original\n+++ New\n@@ -210,7 +210,7 @@\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n-\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n+\t\tif false || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n \t\t\tmissingTables = append(missingTables, tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.48\" with checksum 91916664387742b5ebdde194aa06c46d\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || false {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":213},"diff":"--- Original\n+++ New\n@@ -210,7 +210,7 @@\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n-\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n+\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || false {\n \t\t\ttableCount++\n \t\t} else {\n \t\t\tmissingTables = append(missingTables, tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.49\" with checksum 4c3c3072de7cc7e1302f62c988710f87\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif false || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":213},"diff":"--- Original\n+++ New\n@@ -210,7 +210,7 @@\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n-\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n+\t\tif false || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n \t\t\tmissingTables = append(missingTables, tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.50\" with checksum 83e1c8d4085bc67591a9191070885492\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || false || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":213},"diff":"--- Original\n+++ New\n@@ -210,7 +210,7 @@\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n \t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n \t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n-\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n+\t\tif strings.Contains(upper, needle1) || false || strings.Contains(upper, needle3) {\n \t\t\ttableCount++\n \t\t} else {\n \t\t\tmissingTables = append(missingTables, tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.51\" with checksum 8f794fe1fd90664a37e5f4373f8866d7\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif true \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":373},"diff":"--- Original\n+++ New\n@@ -370,7 +370,7 @@\n \tif err != nil {\n \t\treturn \"\", \"\"\n \t}\n-\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n+\tif true \u0026\u0026 u.Scheme != \"postgresql\" {\n \t\treturn \"\", \"\"\n \t}\n \tif u.User != nil {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.52\" with checksum 5025270cb3e06f011523f2b1ea9f6e41\n"},{"mutator":{"mutatorName":"expression/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 true {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":373},"diff":"--- Original\n+++ New\n@@ -370,7 +370,7 @@\n \tif err != nil {\n \t\treturn \"\", \"\"\n \t}\n-\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n+\tif u.Scheme != \"postgres\" \u0026\u0026 true {\n \t\treturn \"\", \"\"\n \t}\n \tif u.User != nil {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.53\" with checksum 082e525146c727701ff14ad7aa590c02\n"},{"mutator":{"mutatorName":"loop/break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tbreak\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":156},"diff":"--- Original\n+++ New\n@@ -153,7 +153,7 @@\n \tfor _, e := range entries {\n \t\tname := e.Name()\n \t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n-\t\t\tcontinue\n+\t\t\tbreak\n \t\t}\n \t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n \t\t// but guard explicitly in case of future renames).\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.54\" with checksum cf49f1295897d0eac44c52f91124407a\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tbreak\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":0},"diff":"--- Original\n+++ New\n@@ -151,6 +151,7 @@\n \t}\n \tvar files []string\n \tfor _, e := range entries {\n+\t\tbreak\n \t\tname := e.Name()\n \t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n \t\t\tcontinue\n@@ -162,6 +163,7 @@\n \t\t}\n \t\tfiles = append(files, filepath.Join(dir, name))\n \t}\n+\n \tsort.Strings(files)\n \treturn files, nil\n }\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.56\" with checksum a530bbd2de3ec78d9d594f97500c36ff\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tbreak\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":0},"diff":"--- Original\n+++ New\n@@ -176,6 +176,7 @@\n \t}\n \tvar b strings.Builder\n \tfor _, f := range files {\n+\t\tbreak\n \t\tdata, err := os.ReadFile(f)\n \t\tif err != nil {\n \t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n@@ -183,6 +184,7 @@\n \t\tb.Write(data)\n \t\tb.WriteByte('\\n')\n \t}\n+\n \treturn b.String(), nil\n }\n \n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.57\" with checksum 14c16686087d52e971c6b5f2c1856e66\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tbreak\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":0},"diff":"--- Original\n+++ New\n@@ -205,6 +205,7 @@\n \ttableCount := 0\n \tmissingTables := []string{}\n \tfor _, tbl := range RequiredTables {\n+\t\tbreak\n \t\tupperTbl := strings.ToUpper(tbl)\n \t\t// Match with trailing space or newline (word boundary after table name)\n \t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n@@ -216,6 +217,7 @@\n \t\t\tmissingTables = append(missingTables, tbl)\n \t\t}\n \t}\n+\n \tfacts = append(facts, StructuralFact{\n \t\tName:   \"15 required tables\",\n \t\tPassed: tableCount == 15,\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.58\" with checksum acb8966f3df080889a845f4256cd7ccd\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tbreak\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":227},"diff":"--- Original\n+++ New\n@@ -224,6 +224,7 @@\n \n \t// Fact 2: helix_update_updated_at_column function present\n \tfor _, fn := range RequiredFunctions {\n+\t\tbreak\n \t\tupperFn := strings.ToUpper(fn)\n \t\tfacts = append(facts, StructuralFact{\n \t\t\tName:   \"function: \" + fn,\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.59\" with checksum b9041160bc37be3ad49accf56bd4cf1f\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tbreak\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":257},"diff":"--- Original\n+++ New\n@@ -254,6 +254,7 @@\n \n \t// Fact 5: CHECK constraints present\n \tfor _, chk := range RequiredCheckConstraints {\n+\t\tbreak\n \t\tupperChk := strings.ToUpper(chk)\n \t\tfacts = append(facts, StructuralFact{\n \t\t\tName:   \"CHECK: \" + chk,\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.62\" with checksum 77556f963f7fb8f4dc9670cd9c652174\n"},{"mutator":{"mutatorName":"loop/range_break","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tbreak\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":293},"diff":"--- Original\n+++ New\n@@ -290,10 +290,12 @@\n func AllFactsPassed(facts []StructuralFact) (bool, []string) {\n \tvar failures []string\n \tfor _, f := range facts {\n+\t\tbreak\n \t\tif !f.Passed {\n \t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n \t\t}\n \t}\n+\n \treturn len(failures) == 0, failures\n }\n \n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.63\" with checksum 6461285a8720a6baf421566a06e4618a\n"},{"mutator":{"mutatorName":"numbers/decrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(-1)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":103},"diff":"--- Original\n+++ New\n@@ -100,7 +100,7 @@\n // resolved relative to the caller's source file location (works regardless of\n // working directory).\n func SchemaPath() (string, error) {\n-\t_, callerFile, _, ok := runtime.Caller(0)\n+\t_, callerFile, _, ok := runtime.Caller(-1)\n \tif !ok {\n \t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n \t}\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.65\" with checksum b17ca8c18ce02521d3285995f8527b7e\n"},{"mutator":{"mutatorName":"numbers/decrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(-1)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":131},"diff":"--- Original\n+++ New\n@@ -128,7 +128,7 @@\n // (migrations/postgresql/), resolved relative to this source file so it works\n // regardless of the process working directory.\n func MigrationsDir() (string, error) {\n-\t_, callerFile, _, ok := runtime.Caller(0)\n+\t_, callerFile, _, ok := runtime.Caller(-1)\n \tif !ok {\n \t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n \t}\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.66\" with checksum b6e0f4dbe8eded439d74c70698108106\n"},{"mutator":{"mutatorName":"numbers/decrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := -1\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":205},"diff":"--- Original\n+++ New\n@@ -202,7 +202,7 @@\n \t// Fact 1: exactly 15 required CREATE TABLE statements.\n \t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n \t// to avoid false positives from tables whose names are prefixes of other names.\n-\ttableCount := 0\n+\ttableCount := -1\n \tmissingTables := []string{}\n \tfor _, tbl := range RequiredTables {\n \t\tupperTbl := strings.ToUpper(tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.67\" with checksum 3c67dda45ed4018f261803e8fb141a97\n"},{"mutator":{"mutatorName":"numbers/decrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 14,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":221},"diff":"--- Original\n+++ New\n@@ -218,7 +218,7 @@\n \t}\n \tfacts = append(facts, StructuralFact{\n \t\tName:   \"15 required tables\",\n-\t\tPassed: tableCount == 15,\n+\t\tPassed: tableCount == 14,\n \t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n \t})\n \n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.68\" with checksum 8f2c811acee43372990d68d4ebc8e920\n"},{"mutator":{"mutatorName":"numbers/decrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == -1, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":297},"diff":"--- Original\n+++ New\n@@ -294,7 +294,7 @@\n \t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n \t\t}\n \t}\n-\treturn len(failures) == 0, failures\n+\treturn len(failures) == -1, failures\n }\n \n // PSQLConfig holds the connection parameters resolved from the environment.\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.69\" with checksum a3faf925c2338235dfc0c7863fc6f663\n"},{"mutator":{"mutatorName":"numbers/incrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 1\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":205},"diff":"--- Original\n+++ New\n@@ -202,7 +202,7 @@\n \t// Fact 1: exactly 15 required CREATE TABLE statements.\n \t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n \t// to avoid false positives from tables whose names are prefixes of other names.\n-\ttableCount := 0\n+\ttableCount := 1\n \tmissingTables := []string{}\n \tfor _, tbl := range RequiredTables {\n \t\tupperTbl := strings.ToUpper(tbl)\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.72\" with checksum 7a2111f89382c9b28420b47547d814d0\n"},{"mutator":{"mutatorName":"numbers/incrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 16,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":221},"diff":"--- Original\n+++ New\n@@ -218,7 +218,7 @@\n \t}\n \tfacts = append(facts, StructuralFact{\n \t\tName:   \"15 required tables\",\n-\t\tPassed: tableCount == 15,\n+\t\tPassed: tableCount == 16,\n \t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n \t})\n \n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.73\" with checksum 9d5357189ccf86cbc2fbe21d23087b85\n"},{"mutator":{"mutatorName":"numbers/incrementer","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 1, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":297},"diff":"--- Original\n+++ New\n@@ -294,7 +294,7 @@\n \t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n \t\t}\n \t}\n-\treturn len(failures) == 0, failures\n+\treturn len(failures) == 1, failures\n }\n \n // PSQLConfig holds the connection parameters resolved from the environment.\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.74\" with checksum 22089bb83e6f7324bc339a686e8adf79\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\t_, _, _, _, _ = files, files, filepath.Join, dir, name\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":163},"diff":"--- Original\n+++ New\n@@ -160,7 +160,7 @@\n \t\tif name == \"0001_primary_schema.sql\" {\n \t\t\tcontinue\n \t\t}\n-\t\tfiles = append(files, filepath.Join(dir, name))\n+\t\t_, _, _, _, _ = files, files, filepath.Join, dir, name\n \t}\n \tsort.Strings(files)\n \treturn files, nil\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.76\" with checksum db95c764c006042874bb3de4ec79215d\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\t_, _ = b.Write, data\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":183},"diff":"--- Original\n+++ New\n@@ -180,7 +180,7 @@\n \t\tif err != nil {\n \t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n \t\t}\n-\t\tb.Write(data)\n+\t\t_, _ = b.Write, data\n \t\tb.WriteByte('\\n')\n \t}\n \treturn b.String(), nil\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.77\" with checksum 1a89ee4027d24860ec9500510649992f\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\t_, _, _, _, _, _, _, _, _ = facts, facts, Name, Passed, tableCount, Detail, fmt.Sprintf, tableCount, missingTables\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":219},"diff":"--- Original\n+++ New\n@@ -216,11 +216,7 @@\n \t\t\tmissingTables = append(missingTables, tbl)\n \t\t}\n \t}\n-\tfacts = append(facts, StructuralFact{\n-\t\tName:   \"15 required tables\",\n-\t\tPassed: tableCount == 15,\n-\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n-\t})\n+\t_, _, _, _, _, _, _, _, _ = facts, facts, Name, Passed, tableCount, Detail, fmt.Sprintf, tableCount, missingTables\n \n \t// Fact 2: helix_update_updated_at_column function present\n \tfor _, fn := range RequiredFunctions {\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.79\" with checksum fa0cb083ead828a1c319aa9bf4636e3c\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\t_, _,\n\n\t\t// Fact 6: audit_log is partitioned\n\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":264},"diff":"--- Original\n+++ New\n@@ -261,13 +261,10 @@\n \t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n \t\t})\n \t}\n+\t_, _,\n \n-\t// Fact 6: audit_log is partitioned\n-\tfacts = append(facts, StructuralFact{\n-\t\tName:   \"audit_log PARTITION BY RANGE\",\n-\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n-\t\tDetail: \"audit_log must be range-partitioned\",\n-\t})\n+\t\t// Fact 6: audit_log is partitioned\n+\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n \n \t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n \tfacts = append(facts, StructuralFact{\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.80\" with checksum 46f6e9b5d8e135a1ee6e1163911dbd42\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\t_, _,\n\n\t\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":271},"diff":"--- Original\n+++ New\n@@ -268,13 +268,10 @@\n \t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n \t\tDetail: \"audit_log must be range-partitioned\",\n \t})\n+\t_, _,\n \n-\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n-\tfacts = append(facts, StructuralFact{\n-\t\tName:   \"audit_log child partitions\",\n-\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n-\t\tDetail: \"at least one audit_log child partition must be defined\",\n-\t})\n+\t\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n+\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n \n \t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n \tfacts = append(facts, StructuralFact{\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.81\" with checksum d72430a849f58fc08c9a11425c96c63b\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\t_, _,\n\n\t\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":278},"diff":"--- Original\n+++ New\n@@ -275,13 +275,10 @@\n \t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n \t\tDetail: \"at least one audit_log child partition must be defined\",\n \t})\n+\t_, _,\n \n-\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n-\tfacts = append(facts, StructuralFact{\n-\t\tName:   \"helix_nodes_audit trigger definition\",\n-\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n-\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n-\t})\n+\t\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n+\t\t_, _, _, _, _ = facts, facts, Name, Passed, strings.Contains, upper, Detail\n \n \treturn facts\n }\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.82\" with checksum 9668ebe5d43584b3d32be352ecbbe9d2\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, fn, Passed, strings.Contains, upper, upperFn, Detail, fmt.Sprintf, fn\n\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":228},"diff":"--- Original\n+++ New\n@@ -225,11 +225,8 @@\n \t// Fact 2: helix_update_updated_at_column function present\n \tfor _, fn := range RequiredFunctions {\n \t\tupperFn := strings.ToUpper(fn)\n-\t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"function: \" + fn,\n-\t\t\tPassed: strings.Contains(upper, upperFn),\n-\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n-\t\t})\n+\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, fn, Passed, strings.Contains, upper, upperFn, Detail, fmt.Sprintf, fn\n+\n \t}\n \n \t// Fact 3: required trigger names present\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.85\" with checksum f67da2a161d55a8d6ca4e48437fb7d62\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, trig, Passed, strings.Contains, upper, upperTrig, Detail, fmt.Sprintf, trig\n\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":238},"diff":"--- Original\n+++ New\n@@ -235,11 +235,8 @@\n \t// Fact 3: required trigger names present\n \tfor _, trig := range RequiredTriggers {\n \t\tupperTrig := strings.ToUpper(trig)\n-\t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"trigger: \" + trig,\n-\t\t\tPassed: strings.Contains(upper, upperTrig),\n-\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n-\t\t})\n+\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, trig, Passed, strings.Contains, upper, upperTrig, Detail, fmt.Sprintf, trig\n+\n \t}\n \n \t// Fact 4: representative index names\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.86\" with checksum 2d4b9f4b51f986c347ed564b51886fef\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, idx, Passed, strings.Contains, upper, upperIdx, Detail, fmt.Sprintf, idx\n\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":248},"diff":"--- Original\n+++ New\n@@ -245,11 +245,8 @@\n \t// Fact 4: representative index names\n \tfor _, idx := range RequiredIndexPrefixes {\n \t\tupperIdx := strings.ToUpper(idx)\n-\t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"index: \" + idx,\n-\t\t\tPassed: strings.Contains(upper, upperIdx),\n-\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n-\t\t})\n+\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, idx, Passed, strings.Contains, upper, upperIdx, Detail, fmt.Sprintf, idx\n+\n \t}\n \n \t// Fact 5: CHECK constraints present\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.87\" with checksum 2f31f792d43861d2f644feae1ef04cee\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, chk, Passed, strings.Contains, upper, upperChk, Detail, fmt.Sprintf, chk\n\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":258},"diff":"--- Original\n+++ New\n@@ -255,11 +255,8 @@\n \t// Fact 5: CHECK constraints present\n \tfor _, chk := range RequiredCheckConstraints {\n \t\tupperChk := strings.ToUpper(chk)\n-\t\tfacts = append(facts, StructuralFact{\n-\t\t\tName:   \"CHECK: \" + chk,\n-\t\t\tPassed: strings.Contains(upper, upperChk),\n-\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n-\t\t})\n+\t\t_, _, _, _, _, _, _, _, _, _, _ = facts, facts, Name, chk, Passed, strings.Contains, upper, upperChk, Detail, fmt.Sprintf, chk\n+\n \t}\n \n \t// Fact 6: audit_log is partitioned\n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.88\" with checksum 84d3a0ffc55ee1ae54b5adae1bfe8d1b\n"},{"mutator":{"mutatorName":"statement/remove","originalSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\tdb = strings.TrimPrefix(u.Path, \"/\")\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","mutatedSourceCode":"// Package schema provides a thin applier and structural verifier for the\n// Helix Cluster OS authoritative Postgres 16+ primary schema.\n//\n// The SQL migration lives at migrations/postgresql/0001_primary_schema.sql.\n// All DB operations are performed via os/exec → psql (no Go DB driver).\npackage schema\n\nimport (\n\t\"bytes\"\n\t\"fmt\"\n\t\"net/url\"\n\t\"os\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"runtime\"\n\t\"sort\"\n\t\"strings\"\n)\n\n// RequiredTables is the canonical set of 15 table names in the primary schema.\nvar RequiredTables = []string{\n\t\"nodes\",\n\t\"gpu_devices\",\n\t\"sessions\",\n\t\"session_windows\",\n\t\"session_panes\",\n\t\"reservations\",\n\t\"migration_history\",\n\t\"audit_log\",\n\t\"users\",\n\t\"health_snapshots\",\n\t\"llm_advisories\",\n\t\"build_jobs\",\n\t\"build_artifacts\",\n\t\"network_policies\",\n\t\"cluster_config\",\n}\n\n// RequiredFunctions are the two trigger functions whose presence is asserted.\nvar RequiredFunctions = []string{\n\t\"helix_update_updated_at_column\",\n\t\"helix_audit_trigger\",\n}\n\n// RequiredTriggers are the per-table trigger names that must be present.\nvar RequiredTriggers = []string{\n\t\"helix_nodes_updated_at\",\n\t\"helix_nodes_audit\",\n\t\"helix_gpu_devices_updated_at\",\n\t\"helix_sessions_updated_at\",\n\t\"helix_reservations_updated_at\",\n\t\"helix_users_updated_at\",\n\t\"helix_llm_advisories_updated_at\",\n\t\"helix_build_jobs_updated_at\",\n\t\"helix_network_policies_updated_at\",\n\t\"helix_cluster_config_updated_at\",\n}\n\n// RequiredIndexPrefixes are sub-strings that must appear in the SQL for index coverage.\n// These are representative index names to confirm ~53 indexes were written.\nvar RequiredIndexPrefixes = []string{\n\t\"idx_nodes_status\",\n\t\"idx_nodes_role\",\n\t\"idx_nodes_region\",\n\t\"idx_nodes_labels\",\n\t\"idx_nodes_last_seen\",\n\t\"idx_nodes_hostname\",\n\t\"idx_gpu_devices_node_id\",\n\t\"idx_gpu_devices_status\",\n\t\"idx_sessions_owner\",\n\t\"idx_sessions_status\",\n\t\"idx_session_windows_session_id\",\n\t\"idx_session_panes_window_id\",\n\t\"idx_reservations_session_id\",\n\t\"idx_reservations_status\",\n\t\"idx_migration_history_session_id\",\n\t\"idx_audit_log_timestamp\",\n\t\"idx_audit_log_event_type\",\n\t\"idx_users_spiffe_id\",\n\t\"idx_health_snapshots_node_id\",\n\t\"idx_llm_advisories_status\",\n\t\"idx_build_jobs_status\",\n\t\"idx_build_artifacts_job_id\",\n\t\"idx_network_policies_direction\",\n\t\"idx_cluster_config_key\",\n}\n\n// RequiredCheckConstraints are sub-strings that must appear in the SQL for CHECK coverage.\nvar RequiredCheckConstraints = []string{\n\t\"CHECK (cpu_cores \u003e 0)\",\n\t\"CHECK (memory_bytes \u003e 0)\",\n\t\"CHECK (status IN\",\n\t\"CHECK (role IN\",\n\t\"BETWEEN 0 AND 100\",\n\t\"CHECK (confidence \u003e= 0.0\",\n\t\"CHECK (priority \u003e= 0\",\n}\n\n// SchemaPath returns the absolute path to the primary SQL migration file,\n// resolved relative to the caller's source file location (works regardless of\n// working directory).\nfunc SchemaPath() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\t// callerFile is .../internal/schema/schema.go\n\t// migration is at .../migrations/postgresql/0001_primary_schema.sql\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\tp := filepath.Join(repoRoot, \"migrations\", \"postgresql\", \"0001_primary_schema.sql\")\n\treturn filepath.Clean(p), nil\n}\n\n// ReadSQL reads the primary schema SQL file and returns its contents.\nfunc ReadSQL() (string, error) {\n\tp, err := SchemaPath()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tdata, err := os.ReadFile(p)\n\tif err != nil {\n\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", p, err)\n\t}\n\treturn string(data), nil\n}\n\n// MigrationsDir returns the absolute path to the golang-migrate chain directory\n// (migrations/postgresql/), resolved relative to this source file so it works\n// regardless of the process working directory.\nfunc MigrationsDir() (string, error) {\n\t_, callerFile, _, ok := runtime.Caller(0)\n\tif !ok {\n\t\treturn \"\", fmt.Errorf(\"schema: unable to resolve caller path via runtime.Caller\")\n\t}\n\trepoRoot := filepath.Join(filepath.Dir(callerFile), \"..\", \"..\")\n\treturn filepath.Clean(filepath.Join(repoRoot, \"migrations\", \"postgresql\")), nil\n}\n\n// ChainUpFiles returns the ordered list of golang-migrate \"*.up.sql\" files in the\n// numbered chain (001_*.up.sql … NNN_*.up.sql), sorted by version. The\n// consolidated 0001_primary_schema.sql is deliberately EXCLUDED — it is the\n// derived artifact, not a member of the chain.\nfunc ChainUpFiles() ([]string, error) {\n\tdir, err := MigrationsDir()\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tentries, err := os.ReadDir(dir)\n\tif err != nil {\n\t\treturn nil, fmt.Errorf(\"schema: read dir %s: %w\", dir, err)\n\t}\n\tvar files []string\n\tfor _, e := range entries {\n\t\tname := e.Name()\n\t\tif e.IsDir() || !strings.HasSuffix(name, \".up.sql\") {\n\t\t\tcontinue\n\t\t}\n\t\t// Exclude the consolidated derived artifact (it has no \".up.sql\" suffix,\n\t\t// but guard explicitly in case of future renames).\n\t\tif name == \"0001_primary_schema.sql\" {\n\t\t\tcontinue\n\t\t}\n\t\tfiles = append(files, filepath.Join(dir, name))\n\t}\n\tsort.Strings(files)\n\treturn files, nil\n}\n\n// ReadChainSQL reads and concatenates every chain \"*.up.sql\" file in version\n// order, returning the combined SQL text. This is the canonical schema as the\n// golang-migrate runner applies it (see scripts/run-migrations.sh).\nfunc ReadChainSQL() (string, error) {\n\tfiles, err := ChainUpFiles()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\tvar b strings.Builder\n\tfor _, f := range files {\n\t\tdata, err := os.ReadFile(f)\n\t\tif err != nil {\n\t\t\treturn \"\", fmt.Errorf(\"schema: read %s: %w\", f, err)\n\t\t}\n\t\tb.Write(data)\n\t\tb.WriteByte('\\n')\n\t}\n\treturn b.String(), nil\n}\n\n// StructuralFact describes one structural assertion about the SQL text.\ntype StructuralFact struct {\n\tName   string\n\tPassed bool\n\tDetail string\n}\n\n// VerifyStructure parses the SQL text and returns a list of structural facts.\n// All facts must have Passed=true for a valid schema.\nfunc VerifyStructure(sql string) []StructuralFact {\n\tupper := strings.ToUpper(sql)\n\tvar facts []StructuralFact\n\n\t// Fact 1: exactly 15 required CREATE TABLE statements.\n\t// We match \"CREATE TABLE IF NOT EXISTS \u003cname\u003e (\" or \"CREATE TABLE IF NOT EXISTS \u003cname\u003e\\n\"\n\t// to avoid false positives from tables whose names are prefixes of other names.\n\ttableCount := 0\n\tmissingTables := []string{}\n\tfor _, tbl := range RequiredTables {\n\t\tupperTbl := strings.ToUpper(tbl)\n\t\t// Match with trailing space or newline (word boundary after table name)\n\t\tneedle1 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \" \"\n\t\tneedle2 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\n\"\n\t\tneedle3 := \"CREATE TABLE IF NOT EXISTS \" + upperTbl + \"\\r\"\n\t\tif strings.Contains(upper, needle1) || strings.Contains(upper, needle2) || strings.Contains(upper, needle3) {\n\t\t\ttableCount++\n\t\t} else {\n\t\t\tmissingTables = append(missingTables, tbl)\n\t\t}\n\t}\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"15 required tables\",\n\t\tPassed: tableCount == 15,\n\t\tDetail: fmt.Sprintf(\"found %d/15 CREATE TABLE statements; missing: %v\", tableCount, missingTables),\n\t})\n\n\t// Fact 2: helix_update_updated_at_column function present\n\tfor _, fn := range RequiredFunctions {\n\t\tupperFn := strings.ToUpper(fn)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"function: \" + fn,\n\t\t\tPassed: strings.Contains(upper, upperFn),\n\t\t\tDetail: fmt.Sprintf(\"function %q must appear in SQL\", fn),\n\t\t})\n\t}\n\n\t// Fact 3: required trigger names present\n\tfor _, trig := range RequiredTriggers {\n\t\tupperTrig := strings.ToUpper(trig)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"trigger: \" + trig,\n\t\t\tPassed: strings.Contains(upper, upperTrig),\n\t\t\tDetail: fmt.Sprintf(\"trigger %q must appear in SQL\", trig),\n\t\t})\n\t}\n\n\t// Fact 4: representative index names\n\tfor _, idx := range RequiredIndexPrefixes {\n\t\tupperIdx := strings.ToUpper(idx)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"index: \" + idx,\n\t\t\tPassed: strings.Contains(upper, upperIdx),\n\t\t\tDetail: fmt.Sprintf(\"index %q must appear in SQL\", idx),\n\t\t})\n\t}\n\n\t// Fact 5: CHECK constraints present\n\tfor _, chk := range RequiredCheckConstraints {\n\t\tupperChk := strings.ToUpper(chk)\n\t\tfacts = append(facts, StructuralFact{\n\t\t\tName:   \"CHECK: \" + chk,\n\t\t\tPassed: strings.Contains(upper, upperChk),\n\t\t\tDetail: fmt.Sprintf(\"CHECK constraint fragment %q must appear in SQL\", chk),\n\t\t})\n\t}\n\n\t// Fact 6: audit_log is partitioned\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log PARTITION BY RANGE\",\n\t\tPassed: strings.Contains(upper, \"PARTITION BY RANGE\"),\n\t\tDetail: \"audit_log must be range-partitioned\",\n\t})\n\n\t// Fact 7: PARTITION OF audit_log (at least 1 child partition)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"audit_log child partitions\",\n\t\tPassed: strings.Contains(upper, \"PARTITION OF AUDIT_LOG\"),\n\t\tDetail: \"at least one audit_log child partition must be defined\",\n\t})\n\n\t// Fact 8: helix_nodes_audit trigger (immutable audit trigger for nodes)\n\tfacts = append(facts, StructuralFact{\n\t\tName:   \"helix_nodes_audit trigger definition\",\n\t\tPassed: strings.Contains(upper, \"HELIX_NODES_AUDIT\"),\n\t\tDetail: \"the immutable audit trigger on nodes must be present\",\n\t})\n\n\treturn facts\n}\n\n// AllFactsPassed returns true if every StructuralFact has Passed=true.\nfunc AllFactsPassed(facts []StructuralFact) (bool, []string) {\n\tvar failures []string\n\tfor _, f := range facts {\n\t\tif !f.Passed {\n\t\t\tfailures = append(failures, fmt.Sprintf(\"[FAIL] %s: %s\", f.Name, f.Detail))\n\t\t}\n\t}\n\treturn len(failures) == 0, failures\n}\n\n// PSQLConfig holds the connection parameters resolved from the environment.\ntype PSQLConfig struct {\n\t// UseContainer indicates psql should be invoked inside a podman container.\n\tUseContainer  bool\n\tContainerName string\n\tDatabase      string\n\t// User is the role to connect as when no DSN is provided. It is resolved\n\t// from PGUSER (falling back to the DSN's user, then \"helix\"). It MUST NOT be\n\t// silently hardcoded to \"postgres\": the Helix Postgres deployment ships with\n\t// a \"helix\" role and no \"postgres\" role, so a hardcoded \"-U postgres\" fails\n\t// with `role \"postgres\" does not exist` (D11).\n\tUser string\n\t// DSN is the full connection string. When non-empty it is passed verbatim to\n\t// psql (as a positional connection URI) for BOTH local and container\n\t// transports, so psql honors the DSN's host, port, user, dbname and\n\t// password. This is the authoritative connection source.\n\tDSN string\n}\n\n// ResolvePSQLConfig reads environment variables to determine how to connect.\n// Returns (config, available) where available=false means neither container nor DSN is configured.\n//\n// The DSN (PSQL_DSN, falling back to DATABASE_URL) is the authoritative\n// connection source and is threaded into the config for BOTH container and\n// local transports. When no DSN is set, the user/dbname are resolved from\n// PGUSER/PGDATABASE (defaulting to \"helix\" — never \"postgres\").\nfunc ResolvePSQLConfig() (PSQLConfig, bool) {\n\tcontainer := os.Getenv(\"HELIX_PG_CONTAINER\")\n\tdsn := os.Getenv(\"PSQL_DSN\")\n\tif dsn == \"\" {\n\t\tdsn = os.Getenv(\"DATABASE_URL\")\n\t}\n\n\t// Derive defaults from the DSN first, then allow explicit env overrides.\n\tdsnUser, dsnDB := parseDSNUserDB(dsn)\n\n\tdb := os.Getenv(\"PGDATABASE\")\n\tif db == \"\" {\n\t\tdb = dsnDB\n\t}\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\n\tuser := os.Getenv(\"PGUSER\")\n\tif user == \"\" {\n\t\tuser = dsnUser\n\t}\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\n\tif container != \"\" {\n\t\treturn PSQLConfig{UseContainer: true, ContainerName: container, Database: db, User: user, DSN: dsn}, true\n\t}\n\tif dsn != \"\" {\n\t\treturn PSQLConfig{UseContainer: false, DSN: dsn, Database: db, User: user}, true\n\t}\n\treturn PSQLConfig{}, false\n}\n\n// parseDSNUserDB extracts the user and dbname from a postgres connection URI\n// of the form postgres://user[:pass]@host[:port]/dbname[?params]. It returns\n// (\"\", \"\") for inputs that are empty or not a recognizable postgres URI (e.g.\n// libpq keyword/value DSNs), leaving the caller to fall back to env/defaults.\nfunc parseDSNUserDB(dsn string) (user, db string) {\n\tif dsn == \"\" {\n\t\treturn \"\", \"\"\n\t}\n\tu, err := url.Parse(dsn)\n\tif err != nil {\n\t\treturn \"\", \"\"\n\t}\n\tif u.Scheme != \"postgres\" \u0026\u0026 u.Scheme != \"postgresql\" {\n\t\treturn \"\", \"\"\n\t}\n\tif u.User != nil {\n\t\tuser = u.User.Username()\n\t}\n\t_, _, _ = db, strings.TrimPrefix, u.Path\n\treturn user, db\n}\n\n// psqlConnArgs returns the connection-portion psql arguments derived from cfg.\n//\n// When a DSN is present it is passed verbatim as a positional connection URI so\n// psql honors its host/port/user/dbname/password — for BOTH local and container\n// transports. When no DSN is present (container mode invoked purely by name),\n// the user and dbname are taken from cfg.User / cfg.Database — never hardcoded\n// to \"postgres\" (D11). The \"-v ON_ERROR_STOP=1\" verbosity flag is included so\n// every transport fails loudly on the first SQL error.\nfunc psqlConnArgs(cfg PSQLConfig) []string {\n\tif cfg.DSN != \"\" {\n\t\treturn []string{cfg.DSN, \"-v\", \"ON_ERROR_STOP=1\"}\n\t}\n\tuser := cfg.User\n\tif user == \"\" {\n\t\tuser = \"helix\"\n\t}\n\tdb := cfg.Database\n\tif db == \"\" {\n\t\tdb = \"helix\"\n\t}\n\treturn []string{\"-U\", user, \"-d\", db, \"-v\", \"ON_ERROR_STOP=1\"}\n}\n\n// RunSQL executes a SQL string via psql according to PSQLConfig.\n// It returns the combined stdout/stderr output and any error.\nfunc RunSQL(cfg PSQLConfig, sql string) (string, error) {\n\treturn runPsql(cfg, \"-c\", sql)\n}\n\n// RunSQLFile executes a SQL file via psql according to PSQLConfig.\nfunc RunSQLFile(cfg PSQLConfig, filePath string) (string, error) {\n\treturn runPsql(cfg, \"-f\", filePath)\n}\n\n// ApplyPrimarySchema applies the 0001_primary_schema.sql migration via psql.\n//\n// It reads the migration file on the HOST and pipes its contents to psql over\n// stdin (psql -f -). This is required for container mode: `podman exec psql -f\n// \u003chostpath\u003e` would run psql inside the container, which cannot see the host\n// filesystem. Streaming the bytes over stdin works identically for both the\n// container and local-DSN transports.\nfunc ApplyPrimarySchema(cfg PSQLConfig) (string, error) {\n\tsqlText, err := ReadSQL()\n\tif err != nil {\n\t\treturn \"\", err\n\t}\n\treturn RunSQLStdin(cfg, sqlText)\n}\n\n// RunSQLStdin executes SQL piped to psql over stdin (psql -f -), so the SQL\n// bytes do not need to exist as a file inside a target container.\nfunc RunSQLStdin(cfg PSQLConfig, sqlText string) (string, error) {\n\tvar (\n\t\tname    string\n\t\tcmdArgs []string\n\t)\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, \"-f\", \"-\")\n\t} else {\n\t\tname = \"psql\"\n\t\tcmdArgs = append(conn, \"-f\", \"-\")\n\t}\n\tcmd := exec.Command(name, cmdArgs...)\n\tcmd.Stdin = strings.NewReader(sqlText)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql -f -: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// QueryRows runs a SQL query and returns tab-separated rows (one per line).\nfunc QueryRows(cfg PSQLConfig, sql string) ([]string, error) {\n\tout, err := runPsqlQuery(cfg, sql)\n\tif err != nil {\n\t\treturn nil, err\n\t}\n\tvar rows []string\n\tfor _, line := range strings.Split(out, \"\\n\") {\n\t\tline = strings.TrimSpace(line)\n\t\tif line != \"\" {\n\t\t\trows = append(rows, line)\n\t\t}\n\t}\n\treturn rows, nil\n}\n\n// runPsql builds and executes the psql command with the given extra args.\nfunc runPsql(cfg PSQLConfig, extraArgs ...string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, extraArgs...)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// buildPsqlArgs assembles the (executable, args) pair for a psql invocation,\n// honoring the DSN-derived connection for both the local psql transport and the\n// podman-exec container transport. It never hardcodes \"-U postgres\" (D11): the\n// connection portion always comes from psqlConnArgs(cfg).\nfunc buildPsqlArgs(cfg PSQLConfig, extraArgs ...string) (name string, cmdArgs []string) {\n\tconn := psqlConnArgs(cfg)\n\tif cfg.UseContainer {\n\t\tname = \"podman\"\n\t\tcmdArgs = append([]string{\"exec\", \"-i\", cfg.ContainerName, \"psql\"}, conn...)\n\t\tcmdArgs = append(cmdArgs, extraArgs...)\n\t\treturn name, cmdArgs\n\t}\n\tname = \"psql\"\n\tcmdArgs = append(conn, extraArgs...)\n\treturn name, cmdArgs\n}\n\n// runPsqlQuery runs a SQL string and returns the -t -A (tuple-only, unaligned) output.\nfunc runPsqlQuery(cfg PSQLConfig, sql string) (string, error) {\n\tname, cmdArgs := buildPsqlArgs(cfg, \"-t\", \"-A\", \"-c\", sql)\n\tcmd := exec.Command(name, cmdArgs...)\n\tvar buf bytes.Buffer\n\tcmd.Stdout = \u0026buf\n\tcmd.Stderr = \u0026buf\n\tif err := cmd.Run(); err != nil {\n\t\treturn buf.String(), fmt.Errorf(\"psql query: %w\\noutput: %s\", err, buf.String())\n\t}\n\treturn buf.String(), nil\n}\n\n// CheckBinaryPresent returns an error if the named binary is not found in PATH.\nfunc CheckBinaryPresent(name string) error {\n\t_, err := exec.LookPath(name)\n\treturn err\n}\n","originalFilePath":"internal/schema/schema.go","originalStartLine":379},"diff":"--- Original\n+++ New\n@@ -376,7 +376,7 @@\n \tif u.User != nil {\n \t\tuser = u.User.Username()\n \t}\n-\tdb = strings.TrimPrefix(u.Path, \"/\")\n+\t_, _, _ = db, strings.TrimPrefix, u.Path\n \treturn user, db\n }\n \n","processOutput":"PASS \"/Volumes/T7/tmp/go-mutesting-2059653020/internal/schema/schema.go.95\" with checksum 327885a031df9e8b2bc2de24fc7d2e87\n"}],"errored":null}