apiVersion: apps/v1
kind: Deployment
metadata:
  name: helix-policy
  namespace: helix-cluster
  labels:
    app.kubernetes.io/name: helix-policy
    app.kubernetes.io/component: policy
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: helix-policy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: helix-policy
        app.kubernetes.io/component: policy
    spec:
      containers:
        - name: policy
          image: helixcluster/helix-policy:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50058
              name: grpc
          envFrom:
            - configMapRef:
                name: helix-config
          livenessProbe:
            grpc:
              port: 50058
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 50058
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: helix-policy
  namespace: helix-cluster
  labels:
    app.kubernetes.io/name: helix-policy
    app.kubernetes.io/component: policy
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: helix-policy
  ports:
    - port: 50058
      targetPort: 50058
      name: grpc
