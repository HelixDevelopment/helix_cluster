apiVersion: apps/v1
kind: Deployment
metadata:
  name: helix-scheduler
  namespace: helix-cluster
  labels:
    app.kubernetes.io/name: helix-scheduler
    app.kubernetes.io/component: scheduler
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: helix-scheduler
  template:
    metadata:
      labels:
        app.kubernetes.io/name: helix-scheduler
        app.kubernetes.io/component: scheduler
    spec:
      containers:
        - name: scheduler
          image: helixcluster/helix-scheduler:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50052
              name: grpc
          envFrom:
            - configMapRef:
                name: helix-config
          livenessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            grpc:
              port: 50052
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: helix-scheduler
  namespace: helix-cluster
  labels:
    app.kubernetes.io/name: helix-scheduler
    app.kubernetes.io/component: scheduler
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: helix-scheduler
  ports:
    - port: 50052
      targetPort: 50052
      name: grpc
